plugins {
    id 'java-library'
}
group = 'com.scrimmage'
version = '0.0.1'

dependencies {
    implementation project(':common')
    implementation group: 'com.google.code.gson', name: 'gson', version: '2.10.1'
}